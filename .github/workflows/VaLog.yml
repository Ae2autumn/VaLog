name: Deploy VaLog Blog

on:
  push:
    branches: [main]
  issues:
    types: [opened, edited, reopened]
  workflow_dispatch:

# 权限配置（关键）
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install PyYAML markdown requests Jinja2
    
    - name: Generate and Prepare Blog
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        echo "🚀 开始生成并准备 VaLog 博客部署..."
        python VaLog.py --mode auto
        
        # 验证生成的文件
        echo "📁 验证生成的文件..."
        if [ ! -f "docs/index.html" ]; then
          echo "❌ 错误: 未生成 index.html"
          exit 1
        fi
        
        if [ ! -f "docs/.nojekyll" ]; then
          echo "❌ 错误: 未生成 .nojekyll"
          exit 1
        fi
        
        echo "✅ 博客文件验证通过"
    
    - name: Setup Pages
      uses: actions/configure-pages@v4
    
    - name: Upload to Pages
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./docs
    
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
    
    - name: Show Success Message
      run: |
        echo "========================================"
        echo "🎉 VaLog 博客部署成功！"
        echo "========================================"
        echo ""
        echo "🌐 访问地址:"
        echo "  https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        echo ""
        echo "📊 部署环境: github-pages"
        echo "⏰ 部署时间: $(date)"
        echo "========================================"